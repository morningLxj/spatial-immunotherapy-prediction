# Analysis Scripts

This directory provides lightweight reproducibility and integrity-check scripts for the PLOS ONE submission branch.

Primary script:

- `check_source_data.R`

The script verifies that the repository source-data archive is present and writes a source-data inventory. It is an integrity check for the processed source-data package, not a full raw-data re-analysis pipeline.
